# 2GIS Lead Generation System

Система для генерации лидов на основе публичных данных из 2GIS. Приложение работает как поисковая система, позволяющая фильтровать компании по городу и категориям, с последующим экспортом результатов в Excel.

## Возможности

- 🌐 **Веб-интерфейс** - красивый игровой интерфейс в браузере (localhost)
- 🔍 Поиск компаний по городу и категории бизнеса
- 📊 Извлечение данных: название, телефон, адрес, рейтинг, количество отзывов, описание
- 💾 Экспорт результатов в Excel с форматированием
- 🚀 Работа без API ключа - использует публичный доступ к сайту 2GIS
- ⚡ Автоматическая обработка динамического контента
- 📡 Отслеживание прогресса поиска в реальном времени

## Требования

- Python 3.9 или выше
- Google Chrome или Chromium браузер (для Selenium)
- pip для установки зависимостей

## Установка

1. **Клонируйте репозиторий или скачайте файлы проекта**

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение:**
   - Windows: `venv\Scripts\activate`
   - Linux/Mac: `source venv/bin/activate`

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

**Примечание:** При первом запуске `webdriver-manager` автоматически скачает ChromeDriver.

## Использование

### 🌐 Веб-интерфейс (Рекомендуется)

**Самый простой способ использования:**

1. **Двойной клик на `start_web.bat`** (Windows)
   
   ИЛИ вручную:
   ```bash
   python web_app.py
   ```

2. **Откройте браузер:** http://localhost:5000

3. **Заполните форму:**
   - Введите город (например: Москва)
   - Введите категорию (опционально, например: Кафе)
   - Укажите максимум результатов (опционально)

4. **Нажмите "Начать поиск"** и следите за прогрессом в реальном времени

5. **Скачайте результаты** в Excel одним кликом

### 💻 CLI интерфейс (Альтернатива)

Поиск компаний через командную строку:
```bash
python main.py search --city Москва --category Кафе
```

### Параметры команды

- `--city` / `-c` (обязательно) - Название города (например: Москва, Санкт-Петербург)
- `--category` / `-cat` (опционально) - Категория бизнеса (например: Кафе, Рестораны, Салоны красоты)
- `--output` / `-o` (опционально) - Имя выходного Excel файла (по умолчанию: `2gis_results.xlsx`)
- `--max-results` / `-m` (опционально) - Максимальное количество результатов
- `--headless` / `--no-headless` - Запуск браузера в headless режиме (по умолчанию: включен)

### Примеры

**Поиск ресторанов в Санкт-Петербурге:**
```bash
python main.py search --city "Санкт-Петербург" --category Рестораны
```

**Поиск салонов красоты в Москве с ограничением в 50 результатов:**
```bash
python main.py search -c Москва -cat "Салоны красоты" -m 50 -o beauty_salons.xlsx
```

**Поиск всех компаний в Екатеринбурге:**
```bash
python main.py search --city Екатеринбург
```

## Структура выходного файла Excel

Excel файл содержит следующие колонки:

1. **Название компании** - Полное название организации
2. **Телефон** - Контактный телефон
3. **Адрес** - Физический адрес компании
4. **Рейтинг** - Рейтинг компании (если доступен)
5. **Количество голосов** - Количество отзывов/голосов
6. **Информация о компании** - Описание и дополнительная информация
7. **Ссылка** - Прямая ссылка на страницу компании в 2GIS

## Поддерживаемые города

Система поддерживает все города, доступные на сайте 2GIS. Популярные города:
- Москва
- Санкт-Петербург (СПб)
- Екатеринбург
- Новосибирск
- Казань
- Нижний Новгород
- И другие...

## Ограничения и рекомендации

1. **Задержки между запросами:** Система автоматически добавляет задержки между запросами (2-3 секунды) для избежания блокировок.

2. **Производительность:** Скрапинг может быть медленнее, чем использование API, особенно при большом количестве результатов.

3. **Изменения структуры сайта:** Если 2GIS изменит структуру своего сайта, может потребоваться обновление селекторов в коде.

4. **Headless режим:** По умолчанию браузер работает в headless режиме (без GUI). Для отладки можно использовать `--no-headless`.

## Устранение неполадок

### Ошибка "ChromeDriver not found"
- Убедитесь, что установлен Google Chrome
- `webdriver-manager` должен автоматически скачать драйвер при первом запуске
- Проверьте подключение к интернету

### Не найдены компании
- Проверьте правильность написания города и категории
- Попробуйте использовать английские названия для некоторых городов
- Убедитесь, что на сайте 2GIS есть результаты для вашего запроса

### Медленная работа
- Это нормально для веб-скрапинга
- Используйте параметр `--max-results` для ограничения количества результатов
- Убедитесь в стабильном интернет-соединении

## Разработка

### Структура проекта

```
2gis-lead-gen/
├── src/
│   ├── scraper.py         # Веб-скрапер для 2GIS
│   ├── excel_exporter.py  # Экспорт в Excel
│   ├── models.py          # Модели данных
│   └── cli.py             # CLI интерфейс
├── project-docs/          # Документация проекта
├── requirements.txt       # Зависимости
├── README.md              # Этот файл
└── main.py                # Точка входа
```

## Лицензия

Этот проект предназначен для образовательных целей. Убедитесь, что вы соблюдаете условия использования сайта 2GIS при использовании этого инструмента.

## Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте подключение к интернету
4. Убедитесь, что сайт 2GIS доступен
